services:
  helpdesk:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - helpdesk-state:/data
    environment:
      STATE_FILE: /data/state.json
    ports:
      - "3000:3000"

volumes:
  helpdesk-state:
