services:
  helpdesk:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - STATE_FILE=/app/state/state.json
    volumes:
      - helpdesk-state:/app/state
    restart: unless-stopped

volumes:
  helpdesk-state:
