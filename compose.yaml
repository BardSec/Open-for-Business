services:
  helpdesk:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - STATE_FILE=/app/state/state.json
      - NODE_ENV=production
    volumes:
      - helpdesk-state:/app/state
    restart: unless-stopped

volumes:
  helpdesk-state:
